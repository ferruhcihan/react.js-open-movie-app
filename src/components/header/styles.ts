import { Box, styled } from "@mui/material";

export const Wrapper = styled(Box)`
  background: linear-gradient(0deg, transparent 0%, rgba(0, 0, 0, 1) 70%);
  height: 5rem;
  display: flex;
  flex-direction: row;
  align-items: center;

  .icon {
    margin: 1rem;
  }
`;
